{
  "name": "matching-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Balayage périodique des offres de course expirées et relance du matching (docs/08-matching.md) — complète pg_cron, qui ne descend pas sous la minute.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
