# ui

Composants partagés entre l'app passager et l'app chauffeur (thème, boutons,
carte de course, badges de statut) — non initialisé. Le dashboard admin,
techniquement distinct (web, pas React Native), ne consomme pas ce package.
